package com.springcore.core.xml_Compute;

public class Multiplication implements XML_Compute {
    public int compute(int firstNumber, int secondNumber) {
        return firstNumber*secondNumber;
    }
}
